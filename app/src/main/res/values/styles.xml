<resources>

    <!-- Estilo específico para WelcomeActivity -->
    <style name="WelcomeTheme" parent="AppTheme">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="WelcomeButton" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:padding">12dp</item>
        <item name="android:background">@drawable/rounded_button</item>
    </style>

    <!-- Tema base para Wear OS -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Configuración del ActionBar -->
        <item name="windowActionBar">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="actionBarStyle">@style/GradientActionBar</item> <!-- Estilo personalizado -->
        <item name="android:windowBackground">@color/background</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:textColorSecondary">@color/textColorSecondary</item>
    </style>

    <!-- Estilo para el ActionBar con degradado -->
    <style name="GradientActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="background">@drawable/header_gradient</item> <!-- Fondo degradado -->
        <item name="titleTextStyle">@style/CenteredTitleText</item>
        <item name="android:gravity">center</item>
        <item name="displayOptions">showTitle</item>
    </style>

    <!-- Estilo para el título centrado -->
    <style name="CenteredTitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAlignment">center</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- Conserva los demás estilos (MetricText, WearOSScrollStyle, etc.) -->
    <style name="MetricText" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <style name="WearOSScrollStyle" parent="Theme.AppCompat">
        <item name="android:scrollbarThumbVertical">@drawable/custom_scrollbar</item>
        <item name="android:scrollbarSize">4dp</item>
        <item name="android:scrollbarStyle">insideOverlay</item>
    </style>
</resources>